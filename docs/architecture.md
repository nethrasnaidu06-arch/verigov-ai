# Architecture
Mobile (Flutter) and Dashboard (React) -> FastAPI -> PostgreSQL + AI services (YOLO occupancy, anomaly, activity) -> Cross-verification -> Priority engine.
Build order: AI first, then backend, then apps.
